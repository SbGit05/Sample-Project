# Sample React Project

A simple React + Vite starter project.

## Available commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## What this demo includes

- React functional component in `src/App.jsx`
- Local state with `useState`
- Simple button interactions
- Clean Vite setup for fast local dev
